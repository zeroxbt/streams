import { Device, InternetGatewayDevice } from './device.js';
import type { Service } from '@achingbrain/ssdp';
import type { Client, MapPortOptions, UnmapPortOptions } from '../index.js';
export declare class UPNPClient implements Client {
    private closed;
    private readonly discoverGateway;
    static createClient(discoverGateway: () => Promise<Service<InternetGatewayDevice>>): Promise<UPNPClient>;
    constructor(discoverGateway: () => Promise<Service<InternetGatewayDevice>>);
    map(options: MapPortOptions): Promise<void>;
    unmap(options: UnmapPortOptions): Promise<void>;
    externalIp(): Promise<string>;
    findGateway(): Promise<Device>;
    close(): Promise<void>;
}
//# sourceMappingURL=index.d.ts.map